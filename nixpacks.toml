[phases.setup]
nixPkgs = ["...", "postgresql_16.dev"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[phases.build]

[start]
cmd = "gunicorn config.wsgi --bind 0.0.0.0:$PORT --log-file -"
